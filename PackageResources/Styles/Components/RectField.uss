/* RectField */
.appui-rectfield {
    display: flex;
    flex-direction: column;
    align-items: stretch;
    width: 100%;
    /*gap: var(--appui-control-semi-gap);*/
    flex-shrink: 0;
}

.appui-rectfield__row {
    display: flex;
    width: 100%;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    margin-bottom: var(--appui-alias-control-gap-md);
    /*gap: var(--appui-control-gap);*/
}

.appui--rtl .appui-rectfield__row {
    flex-direction: row-reverse;
}

.appui-rectfield__row > .appui-text,
.appui-rectfield__row > .appui-numericalfield {
    flex-basis: 33.33%;
    width: auto;
}

.appui-text.appui-rectfield__label {
    padding-left: var(--appui-spacing-300);
    padding-right: 0;
    -unity-text-align: middle-right;
}

.appui--rtl .appui-text.appui-rectfield__label {
    padding-left: 0;
    padding-right: var(--appui-spacing-300);
    -unity-text-align: middle-left;
}

.appui-rectfield__label,
.appui-rectfield__x-field,
.appui-rectfield__w-field {
    margin-right: var(--appui-alias-control-gap-sm);
    margin-left: 0;
}

.appui--rtl .appui-rectfield__label,
.appui--rtl .appui-rectfield__x-field,
.appui--rtl .appui-rectfield__w-field {
    margin-right: 0;
    margin-left: var(--appui-alias-control-gap-sm);
}